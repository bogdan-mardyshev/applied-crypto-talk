version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: infrastructure/Dockerfile
    volumes:
      - .:/workspace
    tty: true
    stdin_open: true
    ports:
      - "5000:5000"
    working_dir: /workspace
